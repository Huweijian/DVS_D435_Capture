#ifndef INESS_COMMON_TIME_DEFINITIONS_H_
#define INESS_COMMON_TIME_DEFINITIONS_H_

/*! Defines time related types and constants
 */

#include <stdint.h>

namespace iness
{
namespace time
{

typedef int64_t TimeS; // Insightness type for secodns. TODO: Use an actual type to provide type safety.
typedef int64_t TimeMs; // Insightness type for millisecodns. TODO: Use an actual type to provide type safety.
typedef int64_t TimeUs; // Insightness type for microseconds. TODO: Use an actual type to provide type safety.
typedef int64_t TimeNs; // Insightness type for nanoseconds. TODO: Use an actual type to provide type safety.


}
}


#endif
